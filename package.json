{
  "name": "my-bot",
  "scripts": {
    "build": "bun build ./index.js --target bun --compile --outfile app"
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "cron": "^3.1.7",
    "discord.js": "^14.13.0"
  }
}